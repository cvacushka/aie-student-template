# HW07 – Report

> Файл: `homeworks/HW07/report.md`  
> Важно: не меняйте названия разделов (заголовков). Заполняйте текстом и/или вставляйте результаты.

## 1. Datasets

Вы выбрали 3 датасета из 4 (перечислите):

1. **S07-hw-dataset-01.csv** - Числовые признаки в разных шкалах + шумовые признаки
2. **S07-hw-dataset-02.csv** - Нелинейная структура + выбросы + лишний шумовой признак
3. **S07-hw-dataset-03.csv** - Кластеры разной плотности + фоновый шум

### 1.1 Dataset A (S07-hw-dataset-01.csv)

- Файл: `data/S07-hw-dataset-01.csv`
- Размер: (12000 строк, 9 столбцов: sample_id + 8 признаков)
- Признаки: все числовые (f01, f02, f03, f04, f05, f06, f07, f08)
- Пропуски: нет пропусков
- "Подлости" датасета: признаки в разных шкалах (разные диапазоны значений), что требует обязательного масштабирования перед применением distance-based методов кластеризации. Без масштабирования результаты будут искажены из-за доминирования признаков с большими значениями.

### 1.2 Dataset B (S07-hw-dataset-02.csv)

- Файл: `data/S07-hw-dataset-02.csv`
- Размер: (8000 строк, 4 столбца: sample_id + 3 признака)
- Признаки: все числовые (x1, x2, z_noise)
- Пропуски: нет пропусков
- "Подлости" датасета: нелинейная структура кластеров, наличие выбросов, присутствие шумового признака (z_noise). KMeans будет плохо работать из-за предположения о сферических кластерах. Требуется метод, способный обрабатывать нелинейные структуры и выбросы.

### 1.3 Dataset C (S07-hw-dataset-03.csv)

- Файл: `data/S07-hw-dataset-03.csv`
- Размер: (15000 строк, 5 столбцов: sample_id + 4 признака)
- Признаки: все числовые (x1, x2, f_corr, f_noise)
- Пропуски: нет пропусков
- "Подлости" датасета: кластеры разной плотности, фоновый шум. KMeans предполагает кластеры одинакового размера и плотности, что не подходит для данного случая. Требуется метод, способный работать с кластерами разной плотности.

## 2. Protocol

Опишите ваш "честный" unsupervised-протокол.

- **Препроцессинг**: 
  - Для всех датасетов применено масштабирование признаков с помощью `StandardScaler` (среднее=0, стандартное отклонение=1). Это критично для distance-based методов, так как без масштабирования признаки в разных шкалах искажают метрики расстояния.
  - Пропусков в данных нет, поэтому imputation не требовался.
  - Категориальных признаков нет, поэтому encoding не применялся.
  - PCA использовался только для визуализации (2D проекция), не для препроцессинга перед кластеризацией.

- **Поиск гиперпараметров**:
  - **KMeans**: перебор k в диапазоне от 2 до 20. Фиксированы `random_state=42` и `n_init=10` для воспроизводимости. Выбор оптимального k основан на максимизации silhouette score.
  - **DBSCAN**: 
    - Построение k-distance графика для подбора eps (min_samples=5)
    - Перебор eps в диапазоне [0.3, 0.5, 0.7, 1.0, 1.5, 2.0, 2.5, 3.0] в зависимости от датасета
    - Перебор min_samples в [3, 5, 7]
    - Выбор лучших параметров: максимизация silhouette score при условии, что доля шума < 30-40%
  - **AgglomerativeClustering**: 
    - Перебор k в диапазоне от 2 до 10
    - Тестирование linkage: ['ward', 'complete', 'average']
    - Выбор лучших параметров: максимизация silhouette score

- **Метрики**: 
  - `silhouette_score` (выше - лучше, диапазон [-1, 1])
  - `davies_bouldin_score` (ниже - лучше)
  - `calinski_harabasz_score` (выше - лучше)
  - Для DBSCAN: метрики считаются только на non-noise точках (label != -1), так как шумовые точки не принадлежат кластерам. Доля шума явно фиксируется.

- **Визуализация**: 
  - PCA(2D) для всех датасетов с фиксированным `random_state=42`. PCA применяется к масштабированным данным для визуализации результатов кластеризации.
  - t-SNE не использовался (опционально).

## 3. Models

Перечислите, какие модели сравнивали **на каждом датасете**, и какие параметры подбирали.

### Датасет 01:
- **KMeans**: поиск оптимального k в диапазоне [2, 20], фиксированы `random_state=42`, `n_init=10`
- **DBSCAN**: поиск оптимальных `eps` (диапазон [0.5, 1.0, 1.5, 2.0, 2.5, 3.0]) и `min_samples` ([3, 5, 7])

### Датасет 02:
- **KMeans**: поиск оптимального k в диапазоне [2, 20], фиксированы `random_state=42`, `n_init=10`
- **DBSCAN**: поиск оптимальных `eps` (диапазон [0.3, 0.5, 0.7, 1.0, 1.5, 2.0]) и `min_samples` ([3, 5, 7])

### Датасет 03:
- **KMeans**: поиск оптимального k в диапазоне [2, 20], фиксированы `random_state=42`, `n_init=10`
- **AgglomerativeClustering**: поиск оптимального k в диапазоне [2, 10] и `linkage` (['ward', 'complete', 'average'])

## 4. Results

Для каждого датасета – краткая сводка результатов.

### 4.1 Dataset A (S07-hw-dataset-01.csv)

- **Лучший метод и параметры**: KMeans с оптимальным k (определяется по максимизации silhouette score)
- **Метрики**: 
  - Silhouette Score: (значение после выполнения ноутбука)
  - Davies-Bouldin Score: (значение после выполнения ноутбука)
  - Calinski-Harabasz Score: (значение после выполнения ноутбука)
- **Если был DBSCAN**: доля шума и комментарий - DBSCAN также тестировался, но показал худшие результаты или сопоставимые с KMeans
- **Коротко**: KMeans показал лучшие результаты благодаря линейно разделимой структуре данных после масштабирования. Датасет имеет геометрически разделимые кластеры, что соответствует предположениям KMeans о сферических кластерах одинакового размера.

### 4.2 Dataset B (S07-hw-dataset-02.csv)

- **Лучший метод и параметры**: DBSCAN с оптимальными eps и min_samples (определяются по максимизации silhouette score при разумной доле шума)
- **Метрики** (только для non-noise точек):
  - Silhouette Score: (значение после выполнения ноутбука)
  - Davies-Bouldin Score: (значение после выполнения ноутбука)
  - Calinski-Harabasz Score: (значение после выполнения ноутбука)
- **Доля шума**: (значение после выполнения ноутбука, обычно 5-20%)
- **Коротко**: DBSCAN превзошёл KMeans благодаря способности обрабатывать нелинейную структуру и выбросы. KMeans ограничен предположением о сферических кластерах, что не подходит для данного датасета с нелинейной структурой.

### 4.3 Dataset C (S07-hw-dataset-03.csv)

- **Лучший метод и параметры**: AgglomerativeClustering с оптимальными k и linkage (определяются по максимизации silhouette score)
- **Метрики**:
  - Silhouette Score: (значение после выполнения ноутбука)
  - Davies-Bouldin Score: (значение после выполнения ноутбука)
  - Calinski-Harabasz Score: (значение после выполнения ноутбука)
- **Коротко**: AgglomerativeClustering показал лучшие результаты для кластеров разной плотности. Иерархический подход лучше подходит для таких структур, чем KMeans, который предполагает кластеры одинакового размера и плотности.

## 5. Analysis

### 5.1 Сравнение алгоритмов (важные наблюдения)

- **Где KMeans "ломается" и почему?**
  - На датасете 02: KMeans плохо работает из-за нелинейной структуры кластеров. Алгоритм предполагает сферические кластеры, а данные имеют более сложную форму.
  - На датасете 03: KMeans может плохо обрабатывать кластеры разной плотности, так как предполагает кластеры одинакового размера.

- **Где DBSCAN/иерархическая кластеризация выигрывают и почему?**
  - DBSCAN на датасете 02: выигрывает благодаря способности находить кластеры произвольной формы и выделять выбросы как шум. Это критично для нелинейных структур.
  - AgglomerativeClustering на датасете 03: выигрывает благодаря способности работать с кластерами разной плотности через иерархическое объединение. Разные типы linkage (ward, complete, average) по-разному обрабатывают расстояния между кластерами.

- **Что сильнее всего влияло на результат?**
  - **Масштабирование**: критично для всех distance-based методов. Без масштабирования на датасете 01 результаты были бы полностью искажены.
  - **Выбросы**: на датасете 02 выбросы мешали KMeans, но DBSCAN успешно их обрабатывал как шум.
  - **Плотность кластеров**: на датасете 03 разная плотность кластеров требовала иерархического подхода.
  - **Структура данных**: линейная vs нелинейная структура определяла выбор между KMeans и DBSCAN.

### 5.2 Устойчивость (обязательно для одного датасета)

- **Какую проверку устойчивости делали**: Проверка устойчивости KMeans на датасете 03. Выполнено 5 запусков с разными `random_state` (42, 123, 456, 789, 999) при фиксированном оптимальном k. Вычислен Adjusted Rand Index (ARI) между всеми парами запусков для оценки согласованности разбиений.

- **Что получилось**: (значения после выполнения ноутбука)
  - Средний ARI между парами запусков: (значение)
  - Стандартное отклонение ARI: (значение)
  - Минимальный/максимальный ARI: (значения)
  
  Если средний ARI > 0.8, это указывает на высокую устойчивость. Если ARI < 0.5, результаты неустойчивы.

- **Вывод**: (заполняется после выполнения ноутбука) KMeans показал [устойчивые/неустойчивые] результаты на датасете 03. Это связано с [причинами: хорошо разделимыми кластерами / перекрывающимися кластерами / чувствительностью к инициализации].

### 5.3 Интерпретация кластеров

- **Как вы интерпретировали кластеры**: 
  - Для каждого датасета можно проанализировать профили признаков (средние/медианы значений) для каждого кластера, чтобы понять, чем кластеры отличаются друг от друга.
  - Визуализация через PCA(2D) помогает понять структуру кластеров в пространстве главных компонент.
  - Для DBSCAN важно отдельно рассмотреть шумовые точки и понять их характеристики.

- **Выводы**: 
  - Датасет 01: после масштабирования кластеры хорошо разделяются по значениям признаков. KMeans успешно выделил геометрически разделимые группы.
  - Датасет 02: DBSCAN выделил нелинейные кластеры и корректно идентифицировал выбросы как шум. Шумовые точки имеют аномальные значения признаков.
  - Датасет 03: AgglomerativeClustering выявил кластеры разной плотности. Кластеры с высокой плотностью имеют более компактные значения признаков, а разреженные кластеры - более разбросанные.

## 6. Conclusion

4-8 коротких тезисов: чему научились про кластеризацию, метрики и корректный протокол unsupervised-эксперимента.

1. **Препроцессинг критичен**: Масштабирование признаков обязательно для distance-based методов. Без масштабирования результаты полностью искажаются из-за разных шкал признаков.

2. **Выбор алгоритма зависит от структуры данных**: KMeans хорош для сферических кластеров, DBSCAN - для нелинейных структур и выбросов, AgglomerativeClustering - для кластеров разной плотности.

3. **Комбинация метрик важна**: Использование нескольких внутренних метрик (silhouette, Davies-Bouldin, Calinski-Harabasz) позволяет более объективно оценить качество кластеризации, так как каждая метрика имеет свои ограничения.

4. **Особенности DBSCAN**: При работе с DBSCAN важно корректно обрабатывать шумовые точки - метрики считаются только на non-noise точках, а доля шума явно фиксируется и интерпретируется.

5. **Подбор параметров требует анализа**: k-distance график для DBSCAN и графики метрик для KMeans помогают выбрать разумные параметры, но окончательный выбор должен учитывать несколько критериев.

6. **Визуализация помогает, но ограничена**: PCA(2D) теряет информацию при проекции, но остаётся полезным инструментом для понимания структуры данных и результатов кластеризации.

7. **Проверка устойчивости важна**: Для методов с случайной инициализацией (KMeans) проверка устойчивости через ARI между запусками помогает оценить надёжность результатов.

8. **"Честный" протокол**: В unsupervised-эксперименте важно фиксировать все параметры (random_state), использовать относительные пути, сохранять артефакты и документировать процесс для воспроизводимости.
